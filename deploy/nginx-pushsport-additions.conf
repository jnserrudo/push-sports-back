# Fragmentos para AGREGAR al server 443 de pushsport.com.ar
# No reemplaza el archivo actual. Siempre: sudo nginx -t && sudo systemctl reload nginx

# --- Mantenimiento (activar mientras se trabaja) ---
# map $cookie_push_maint $is_maint_bypass {
#     default 0;
#     "PONER_SECRETO_AQUI" 1;
# }
#
# location / {
#     if ($is_maint_bypass = 0) {
#         rewrite ^ /maintenance.html last;
#     }
#     try_files $uri $uri/ /index.html;
# }
# location = /maintenance.html {
#     root /var/www/push-sports-front;
# }

# --- API del Node en el VPS (ajustar puerto de PM2) ---
# location /api/ {
#     proxy_pass http://127.0.0.1:3003;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }

# --- Imágenes locales ---
# location /uploads/ {
#     alias /var/www/push-sports-uploads/;
#     access_log off;
#     expires 30d;
# }
